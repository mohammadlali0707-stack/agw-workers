name: relay-deploy-file

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest  # self-hosted disabled, owner instruction 2026-09-19: runner-api.py fleet has no free capacity/API for this repo -- back to GitHub-hosted until further notice
    env:
      ACC0_PAT: ${{ secrets.ACC0_PAT }}
      ACC1_PAT: ${{ secrets.ACC1_PAT }}
      ACC2_PAT: ${{ secrets.ACC2_PAT }}
      ACC3_PAT: ${{ secrets.ACC3_PAT }}
      ACC4_PAT: ${{ secrets.ACC4_PAT }}
      ACC5_PAT: ${{ secrets.ACC5_PAT }}
      ACC7_PAT: ${{ secrets.ACC7_PAT }}
      ACC8_PAT: ${{ secrets.ACC8_PAT }}
    steps:
      - uses: actions/checkout@v4

      # Account 0 (Mohammadlali) added 2026-09-15: previously excluded here
      # on the assumption it had no separate Actions-capable copy of this
      # repo -- wrong, disproved live by a relay-read-file.yml diagnostic
      # (Mohammadlali/agw-workers exists, is reachable, and has its own
      # .github/workflows/ with a real agw-worker.yml already in it, just
      # never kept in sync by this relay). agy-lead-plan.yml added to the
      # list: agy-plan-dispatch.yml now cross-dispatches each account's
      # lead to run inside that account's own repo (see its own comment for
      # why), which requires this file to exist there first.
      - name: Deploy shared files to the other 8 accounts' agw-workers
        run: |
          set -euo pipefail
          FILES=(
            ".github/workflows/media-request-bot.yml"
            ".github/workflows/agw-claude.yml"
            ".github/workflows/agy-issue-bot.yml"
            ".github/workflows/agw-worker.yml"
            ".github/workflows/agy-lead-plan.yml"
            "media/README.md"
            "README.md"
          )
          declare -A OWNERS=(
            [0]="Mohammadlali"
            [1]="momonakikugava-pixel"
            [2]="lali94m-max"
            [3]="ngocgminh5-debug"
            [4]="hmmletssee7-design"
            [5]="kidding602"
            [7]="mohammad97okk"
            [8]="moradzahra85-png"
          )
          for IDX in 0 1 2 3 4 5 7 8; do
            OWNER="${OWNERS[$IDX]}"
            PAT_NAME="ACC${IDX}_PAT"
            TOKEN="${!PAT_NAME}"
            echo "== account $IDX: $OWNER/agw-workers =="
            if [ -z "$TOKEN" ]; then
              echo "::warning::no PAT for account $IDX ($OWNER), skipping"
              continue
            fi
            AUTH_HEADER="AUTHORIZATION: basic $(printf 'x-access-token:%s' "$TOKEN" | base64 -w0)"
            WORKDIR=$(mktemp -d)
            if ! git -c http.https://github.com/.extraHeader="$AUTH_HEADER" clone --depth 1 \
                "https://github.com/${OWNER}/agw-workers.git" "$WORKDIR"; then
              echo "::warning::clone failed for $OWNER/agw-workers, skipping"
              rm -rf "$WORKDIR"
              continue
            fi
            for F in "${FILES[@]}"; do
              mkdir -p "$WORKDIR/$(dirname "$F")"
              cp "$F" "$WORKDIR/$F"
            done
            (
              cd "$WORKDIR"
              git config user.name "media-request-bot-deploy"
              git config user.email "actions@users.noreply.github.com"
              git add "${FILES[@]}"
              if git diff --cached --quiet; then
                echo "no changes for $OWNER, skipping push"
              else
                git commit -m "Deploy shared files from mohammadlali0707-stack/agw-workers (relay)"
                git -c http.https://github.com/.extraHeader="$AUTH_HEADER" push origin HEAD
                echo "pushed to $OWNER/agw-workers"
              fi
            )
            rm -rf "$WORKDIR"
          done
