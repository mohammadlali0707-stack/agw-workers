name: AGY Project Planning Bot (propose an account-level breakdown, do not dispatch)

# Triggered by a message ending in "@agy-plan" (distinct from the existing
# "@agy" single-task marker in agy-issue-bot.yml, so this does not collide
# with that actively-evolving pipeline). Reads the target project's own
# Team/org.json roster + Team/MAP.md state, then asks agy to PROPOSE an
# ACCOUNT-LEVEL breakdown -- up to 8 packages, one per relevant account,
# each a self-contained scope for that account's own lead to break down
# further among its 10 subordinates (see agy-lead-plan.yml), never
# dispatched from here. Posted as a comment containing a fenced ```json
# block a human (or the dashboard UI) reviews before anything runs. See
# agy-plan-dispatch.yml for the workflow that dispatches one lead per
# account on approval, and agy-lead-plan.yml for what each lead then does.
#
# Same trigger contract as agy-issue-bot.yml's own fix earlier today:
# whitespace/case-tolerant trailing-marker check in a real bash job (not a
# GitHub Actions expression, which has no escape sequences), and
# author_association OWNER/COLLABORATOR or login == Mohammadlali (the
# dashboard chat's own account, which has no relationship to this repo).

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

permissions:
  issues: write
  actions: write
  contents: read

jobs:
  check-marker:
    runs-on: ubuntu-latest  # self-hosted disabled, owner instruction 2026-09-19: runner-api.py fleet has no free capacity for this repo -- back to GitHub-hosted until further notice
    timeout-minutes: 5
    # ACCEPTS BOTH EVENTS THIS WORKFLOW SUBSCRIBES TO.
    # `on:` declares `issues: [opened]` AND `issue_comment`, the `check` step
    # below already reads `comment.body || issue.body`, and the `propose` job
    # already branches on both -- but THIS gate required
    # `event_name == 'issue_comment'`, so an issue OPENED could never get past
    # it. Measured 2026-09-19: the Notion poller created issue #42 from a real
    # Notion page, correctly ending in `ccp @agy-plan`; agy-plan-bot fired 3
    # seconds later on the `issues` event and reported **skipped**. The whole
    # Notion -> GitHub -> plan chain stopped here, on a workflow that
    # subscribes to the event and then refuses it.
    if: >
      (github.event_name == 'issue_comment' &&
        contains(github.event.comment.body, '@agy-plan') &&
        !contains(github.event.comment.body, '@agy-plan-approved')) ||
      (github.event_name == 'issues' &&
        contains(github.event.issue.body, '@agy-plan') &&
        !contains(github.event.issue.body, '@agy-plan-approved'))
    outputs:
      match: ${{ steps.check.outputs.match }}
      project_tag: ${{ steps.check.outputs.project_tag }}
    steps:
      - name: Check for a trailing @agy-plan marker and extract the project tag
        id: check
        env:
          BODY: ${{ github.event.comment.body || github.event.issue.body || '' }}
          TITLE: ${{ github.event.issue.title || '' }}
        run: |
          # STRIP A TRAILING ATTRIBUTION FOOTER BEFORE TESTING THE MARKER.
          # Measured 2026-09-18 on issue #38: a task comment written by a
          # Claude Code session ends up with
          #   ---
          #   _Generated by [Claude Code](https://claude.ai/code)_
          # appended as the LAST lines, by the platform, after the body is
          # submitted -- confirmed on two independent routes (the MCP tool and
          # a raw urllib POST with a PAT), and a PATCH that placed the footer
          # mid-body had it moved back to the end. So the author cannot keep
          # the marker last, and the marker test below silently never matched:
          # the comment looked correct to a human and triggered nothing.
          # This is not a loosening. The marker must still be the last thing in
          # the body once a trailing footer is removed -- a marker buried in the
          # middle of a comment still does not count.
          strip_footer() {
            python3 - "$1" <<'PYEOF'
          import re, sys
          text = sys.argv[1]
          lines = text.split("\n")
          pat = re.compile(
              r"^\s*(-{3,}|\*{3,}|_?Generated by \[[^\]]*\]\([^)]*\)_?|"
              r"<!--.*?-->|Co-Authored-By:.*|Claude-Session:.*|\s*)\s*$")
          while lines and pat.match(lines[-1]):
              lines.pop()
          sys.stdout.write("\n".join(lines))
          PYEOF
          }
          TRIMMED="$(printf '%s\n%s' "$TITLE" "$BODY" | sed -e 's/[[:space:]]*$//')"
          TRIMMED="$(strip_footer "$TRIMMED")"
          LOWER="$(printf '%s' "$TRIMMED" | tr '[:upper:]' '[:lower:]')"
          case "$LOWER" in
            *"@agy-plan")
              echo "match=true" >> "$GITHUB_OUTPUT"
              PROJECT=$(echo "$LOWER" | grep -oE '(ccp|cr|coffeenet|status)[[:space:]]+@agy-plan$' | awk '{print $1}')
              echo "project_tag=${PROJECT:-ccp}" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "match=false" >> "$GITHUB_OUTPUT"
              ;;
          esac

  propose:
    needs: [check-marker]
    runs-on: ubuntu-latest  # self-hosted disabled, owner instruction 2026-09-19: runner-api.py fleet has no free capacity for this repo -- back to GitHub-hosted until further notice
    timeout-minutes: 30
    if: >
      (github.event_name == 'issues' &&
        needs.check-marker.outputs.match == 'true' &&
        (github.event.issue.author_association == 'OWNER' ||
          github.event.issue.author_association == 'COLLABORATOR' ||
          github.event.issue.user.login == 'Mohammadlali')
      ) ||
      (github.event_name == 'issue_comment' &&
        needs.check-marker.outputs.match == 'true' &&
        github.event.comment.user.type != 'Bot' &&
        (github.event.comment.author_association == 'OWNER' ||
          github.event.comment.author_association == 'COLLABORATOR' ||
          github.event.comment.user.login == 'Mohammadlali')
      )
    env:
      # Unique per job execution. /tmp/agent-repo was a single shared path:
      # it survived between jobs (which is why a `rm -rf` was added on
      # 2026-09-17), and `rm -rf` on a SHARED path is only safe when jobs run
      # one at a time. They do not -- agy-lead-plan dispatches 10 agw-worker
      # runs 5s apart, so two on one host would delete each other's checkout
      # mid-clone. That failure is silent, unlike the dir-exists error it
      # replaced. runner.temp is per-runner and the run/attempt/job triple
      # makes it unique even if the temp dir is ever shared.
      # AGENT_REPO is set by the first step below. The `runner` context is
      # NOT available in a job-level env: block -- only in step-level ones --
      # so `${{ runner.temp }}` here made GitHub reject the whole workflow at
      # startup: zero jobs, no log, the same signature as an unparseable file.
      # 74f136f shipped exactly that in four files; d99dbc0 fixed one of them.
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ACC6_PAT: ${{ secrets.ACC6_PAT }}
      PROJECT_TAG: ${{ needs.check-marker.outputs.project_tag }}
    steps:
      # Needed for Tools/agy_checklist.py. This job did not check out its own
      # repo before, because everything it used lived in the workflow file --
      # so the first draft of the checklist wiring below called a tool that was
      # not on disk.
      - uses: actions/checkout@v4

      - name: Set unique AGENT_REPO path
        run: |
          echo "AGENT_REPO=${RUNNER_TEMP}/agent-repo-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-propose" >> "$GITHUB_ENV"

      - name: Resolve the project tag to a target repo
        id: resolve
        run: |
          case "$PROJECT_TAG" in
            ccp) echo "target_repo=mohammadlali0707-stack/Claud-Cloud-Project" >> "$GITHUB_OUTPUT" ;;
            cr) echo "target_repo=mohammadlali0707-stack/Control-Room" >> "$GITHUB_OUTPUT" ;;
            coffeenet) echo "target_repo=mohammadlali0707-stack/AirboxVIP_Coffeenet" >> "$GITHUB_OUTPUT" ;;
            status) echo "target_repo=mohammadlali0707-stack/status-dashboard" >> "$GITHUB_OUTPUT" ;;
            *) echo "target_repo=mohammadlali0707-stack/Claud-Cloud-Project" >> "$GITHUB_OUTPUT" ;;
          esac

      # THE CHECKLIST, POSTED BEFORE ANY WORK STARTS.
      # Owner instruction 2026-09-18: "the bot must write a checklist of what
      # it is going to do before it sends the full answer, and tick each item
      # as it finishes -- solve this at the root."
      # It WAS solved, but only for the lead tier: agy-lead-plan.yml has called
      # Tools/agy_checklist.py since the checklist was built, and five live
      # checklists exist on issue #38 for plan-38-acc1/acc2/acc7/... The
      # planner -- the bot the owner actually watched -- was never wired in. It
      # posted a one-line "preparing a proposal..." and then went silent for
      # minutes before dropping ~5000 characters in one block.
      # The root fix is not a second mechanism. It is the same tool, made cheap
      # enough to call from any step (`tick`, one line), and then actually
      # called here. The items are THIS JOB'S OWN steps, known before the model
      # is asked anything, so the list is honest at the moment it is posted
      # rather than being a promise the model might not keep.
      - name: Post the work checklist before doing any of it
        env:
          CHECKLIST_MARKER: "<!-- agy-checklist:plan-bot-${{ github.run_id }} -->"
          CHECKLIST_ISSUE: ${{ github.event.issue.number }}
        run: |
          echo "CHECKLIST_MARKER=${CHECKLIST_MARKER}" >> "$GITHUB_ENV"
          echo "CHECKLIST_ISSUE=${CHECKLIST_ISSUE}" >> "$GITHUB_ENV"
          echo "CHECKLIST_DONE=${RUNNER_TEMP}/plan_bot_done.txt" >> "$GITHUB_ENV"
          : > "${RUNNER_TEMP}/plan_bot_done.txt"
          printf '%s\t%s\t%s\n' \
            "plan-checkout" "خواندن مخزن هدف" "${{ steps.resolve.outputs.target_repo }}" \
            "plan-thread" "خواندن کل گفتگوی این ایشو" "planner" \
            "plan-draft" "تدوین پروپوزال با agy" "planner" \
            "plan-post" "ارسال پروپوزال به‌عنوان کامنت" "planner" \
            > /tmp/plan_bot_tasks.tsv
          python3 "$GITHUB_WORKSPACE/Tools/agy_checklist.py" post \
            --repo "${{ github.repository }}" \
            --issue "$CHECKLIST_ISSUE" \
            --marker "$CHECKLIST_MARKER" \
            --title "Project Manager — نقشه‌ی کار برای ${{ steps.resolve.outputs.target_repo }}" \
            --tasks /tmp/plan_bot_tasks.tsv

      - name: Checkout target repo
        env:
          TARGET_REPO: ${{ steps.resolve.outputs.target_repo }}
        run: |
          set -e
          if [ -z "$ACC6_PAT" ]; then
            echo "::error::ACC6_PAT not set -- cannot check out $TARGET_REPO"
            exit 1
          fi
          AUTH_HEADER="AUTHORIZATION: basic $(printf 'x-access-token:%s' "$ACC6_PAT" | base64 -w0)"
          rm -rf "${AGENT_REPO}"
          mkdir -p "${AGENT_REPO}"
          git -c http.https://github.com/.extraHeader="$AUTH_HEADER" clone \
            "https://github.com/${TARGET_REPO}.git" "${AGENT_REPO}"


      - name: Tick "plan-checkout"
        run: |
          python3 "$GITHUB_WORKSPACE/Tools/agy_checklist.py" tick \
            --repo "${{ github.repository }}" \
            --issue "$CHECKLIST_ISSUE" \
            --marker "$CHECKLIST_MARKER" \
            --done-file "$CHECKLIST_DONE" --tag "plan-checkout" || true

      - name: Install AGY CLI from HF Dataset
        run: |
          curl -L \
            "https://huggingface.co/datasets/stranger777777/agw-cli-bin/resolve/main/cli_linux_x64.tar.gz" \
            -o /tmp/cli.tar.gz
          mkdir -p /tmp/agx
          tar -xzf /tmp/cli.tar.gz -C /tmp/agx
          CLI_BIN=$(find /tmp/agx -type f \( -name "antigravity" -o -name "agy" \) | head -1)
          if [ -z "$CLI_BIN" ]; then
            CLI_BIN=$(find /tmp/agx -type f -executable | head -1)
          fi
          # Install without sudo. Ephemeral runners are fresh containers and do
          # not inherit /etc/sudoers.d/runner from the Arvan host, so
          # `sudo install` dies with "sudo: a password is required" -- measured
          # 2026-09-17T09:58:21Z in job 105158016308, after the sudoers fix was
          # believed to be in place. A user-writable dir added to $GITHUB_PATH
          # needs no privilege and behaves identically on self-hosted and on
          # ubuntu-latest, so this no longer depends on the runner image.
          AGY_BIN_DIR="${RUNNER_TEMP:-/tmp}/agy-bin"
          mkdir -p "$AGY_BIN_DIR"
          install -m 0755 "$CLI_BIN" "$AGY_BIN_DIR/agy"
          # $GITHUB_PATH only affects LATER steps, so export for this one too.
          echo "$AGY_BIN_DIR" >> "$GITHUB_PATH"
          export PATH="$AGY_BIN_DIR:$PATH"
          agy --version 2>/dev/null || echo "agy ready"

      - name: Restore this repo's own AGY identity (planning is read-only, no fallback needed)
        run: |
          mkdir -p ~/.gemini/antigravity-cli ~/.gemini/config
          echo "${{ secrets.AGW_TOKEN_ACC6 }}" | base64 -d > ~/.gemini/antigravity-cli/antigravity-oauth-token
          chmod 600 ~/.gemini/antigravity-cli/antigravity-oauth-token
          cat /proc/sys/kernel/random/uuid > ~/.gemini/antigravity-cli/installation_id
          echo '{"userSettings":{}}' > ~/.gemini/config/config.json

      - name: Fetch the full issue thread for context (matters on a revision round)
        id: thread
        run: |
          gh issue view "${{ github.event.issue.number }}" \
            --repo "${{ github.repository }}" \
            --json title,body,comments \
            --jq '"ISSUE: " + .title + "\n" + .body + "\n\n" + ([.comments[] | "--- COMMENT by " + .author.login + " ---\n" + .body] | join("\n\n"))' \
            > /tmp/thread.txt || echo "(could not fetch thread)" > /tmp/thread.txt


      - name: Tick "plan-thread"
        run: |
          python3 "$GITHUB_WORKSPACE/Tools/agy_checklist.py" tick \
            --repo "${{ github.repository }}" \
            --issue "$CHECKLIST_ISSUE" \
            --marker "$CHECKLIST_MARKER" \
            --done-file "$CHECKLIST_DONE" --tag "plan-thread" || true

      - name: Build the planning prompt
        id: prompt
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          TARGET_REPO: ${{ steps.resolve.outputs.target_repo }}
        run: |
          sanitize() { sed "s/\`/'/g; s/\"/'/g; s/\$(/DOLLAR(/g; s/\${/DOLLAR{/g"; }
          SAFE_TITLE=$(printf '%s' "$ISSUE_TITLE" | tr -d '`\\$')
          SAFE_BODY=$(printf '%s' "$ISSUE_BODY" | sanitize)
          SAFE_COMMENT=$(printf '%s' "$COMMENT_BODY" | sanitize)
          GOAL="$SAFE_BODY"
          IS_REVISION=false
          if [ -n "$SAFE_COMMENT" ]; then GOAL="$SAFE_COMMENT"; IS_REVISION=true; fi
          {
            printf '%s\n' 'You are the Project Manager AI for this fleet. This is a PLANNING request only.'
            printf '%s\n' 'Do NOT write, edit, or commit any files. Do NOT perform any of the tasks yourself. Do NOT run any dispatch command. Only read and propose.'
            if [ "$IS_REVISION" = "true" ]; then
              printf '\n%s\n' 'This is a REVISION round. Below is the full conversation so far, including your own earlier proposal(s) and any @agy-plan-approved marker (ignore that marker, it is not addressed to you). Read it, understand what was already proposed and why the requester wants it changed (their newest message is the actual revision instruction), then produce a NEW, complete, corrected proposal -- not a diff or a partial patch to the old one.'
              printf '\n--- CONVERSATION SO FAR ---\n'
              cat /tmp/thread.txt | sanitize
              printf '\n--- END CONVERSATION ---\n'
            fi
            printf '\nGOAL / LATEST INSTRUCTION (may include a leading project tag and a trailing marker -- ignore both, they are routing metadata, not part of the goal):\n'
            printf '%s\n' "$GOAL"
            printf '\nDo this, in order:\n'
            printf '%s\n' '1. Read Team/org.json in this checked-out repo (if present) for the full member roster: each entry has id, account (ACC0-ACC8; ACC6 and ACC9 never appear here), role, guild, domain_is, title. If Team/org.json does not exist in this repo, say so plainly in your prose summary and propose a flat task list instead (no member_id/account fields) rather than inventing IDs.'
            printf '%s\n' '2. Read Team/MAP.md'"'"'s CURRENT STATE block (top of file) for what is already done, blocked, or in progress, so you do not propose duplicate or already-finished work.'
            printf '%s\n' '3. Read the repo'"'"'s README and the most recent entries of Team/CHANGELOG.md (if present) for scope and recent history.'
            printf '%s\n' '4. Break the GOAL into up to 8 ACCOUNT-LEVEL packages -- NOT per-member tasks. Team/org.json has an "accounts" map with entries ACC0..ACC8 (ACC6 and ACC9 never appear as work targets); each has a "lead" (its w01 member) plus a primary/secondary domain. For each account whose domain is actually relevant to this GOAL, write ONE package: a self-contained scope of work for that account'"'"'s TEAM (its lead plus 10 subordinates) to plan and execute on its own -- not individual task instructions. Skip an account entirely if this GOAL genuinely has no work for its domain; do not force all 8 to have a package.'
            printf '\n%s\n' '5. Output your ENTIRE response as exactly two parts, nothing else:'
            printf '%s\n' '   a) A short Persian paragraph (3-6 sentences) summarizing the plan in plain language, for a human to read.'
            printf '%s\n' '   b) Then ONE fenced code block, language json, containing ONLY valid JSON with exactly this shape (no comments, no trailing text inside the fence):'
            printf '%s\n' '{"kind":"tbs_account_plan","project_tag":"'"$PROJECT_TAG"'","target_repo":"'"$TARGET_REPO"'","goal":"<short restatement>","packages":[{"account":<int 0-8, never 6 or 9>,"package_goal":"<a full, self-contained scope of work for this account'"'"'s lead to break down further among its own 10 subordinates -- concrete enough to act on, not vague>"}]}'
            printf '%s\n' 'Do not include a trailing @agy or @agy-plan marker anywhere in your response -- this response is a proposal, not a new request.'
          } > /tmp/prompt.txt
          echo "prompt_file=/tmp/prompt.txt" >> "$GITHUB_OUTPUT"

      - name: Run AGY in planning mode
        id: run_agy
        run: |
          set +e
          cd "${AGENT_REPO}" 2>/dev/null || true
          PROMPT=$(cat /tmp/prompt.txt)
          OUTPUT=$(timeout 1700 agy --dangerously-skip-permissions --print-timeout 1680s -p "$PROMPT" 2>&1)
          echo "$OUTPUT" > /tmp/agy_output.txt
          echo "Output lines: $(wc -l < /tmp/agy_output.txt)"


      - name: Tick "plan-draft"
        run: |
          python3 "$GITHUB_WORKSPACE/Tools/agy_checklist.py" tick \
            --repo "${{ github.repository }}" \
            --issue "$CHECKLIST_ISSUE" \
            --marker "$CHECKLIST_MARKER" \
            --done-file "$CHECKLIST_DONE" --tag "plan-draft" || true

      - name: Post the proposal as a comment
        run: |
          if [ ! -s /tmp/agy_output.txt ]; then
            gh issue comment "${{ github.event.issue.number }}" \
              --repo "${{ github.repository }}" \
              --body "Project Manager: پروپوزال ساخته نشد (خروجی خالی). دوباره تلاش کنید."
            exit 0
          fi
          gh issue comment "${{ github.event.issue.number }}" \
            --repo "${{ github.repository }}" \
            --body-file /tmp/agy_output.txt

      - name: Tick "plan-post"
        run: |
          python3 "$GITHUB_WORKSPACE/Tools/agy_checklist.py" tick \
            --repo "${{ github.repository }}" \
            --issue "$CHECKLIST_ISSUE" \
            --marker "$CHECKLIST_MARKER" \
            --done-file "$CHECKLIST_DONE" --tag "plan-post" || true
